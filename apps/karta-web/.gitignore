node_modules/
dist/
.vite/
*.tsbuildinfo

# Playwright e2e
test-results/
playwright-report/
e2e/screenshots/

# Synced from apps/web pipeline — gitignored to avoid duplicating large geojson
# in two trees. Build CI runs sync-data.mjs before vite build.
public/data/
public/logos/

# Generated at build time from lookup files — regenerated by build-sitemap.mjs.
public/sitemap.xml
public/robots.txt

# Local env / IDE noise
.env
.env.local
.DS_Store
